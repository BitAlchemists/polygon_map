part of stagexl_plus;

class BitmapData extends stagexl.BitmapData {
  BitmapData(int width, int height) : super(width, height);
  
  perlinNoise(num baseX, num baseY, num numOctaves, num randomSeed, bool stitch, bool fractalNoise, [num channelOptions, bool grayScale, Map offsets]){
    
  }
  
  noise(num randomSeed, [num low, num high, num channelOptions, bool grayScale]){
    
  }

}